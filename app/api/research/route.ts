import { NextRequest } from "next/server";
export async function POST(request:NextRequest){const key=process.env.BITGET_QWEN_API_KEY;if(!key)return Response.json({error:"Qwen is not configured in this deployment yet."},{status:503});const body=await request.json();const prompt=`You are Veyra ClosePrint, a session-aware rToken research analyst. Produce a concise research brief from the supplied event packet and deterministic market metrics. Separate the response into: Event read, What the weekend book already reflects, Tradability at the stated size, and Invalidation. Clearly label illustrative, user-supplied, observed, derived, and modeled inputs. Never invent consensus figures, historical analogues, or live observations. Do not predict a price or place/tell the user to place an order. The human makes the final decision. Maximum 180 words.\n\n${JSON.stringify(body)}`;try{const response=await fetch("https://hackathon.bitgetops.com/v1/responses",{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${key}`},body:JSON.stringify({model:"qwen3.8-max",input:prompt})});const json=await response.json();if(!response.ok)throw new Error(json.error?.message||"Qwen request failed.");const output=json.output_text??json.output?.flatMap((item:{content?:Array<{text?:string}>})=>item.content??[]).map((item:{text?:string})=>item.text??"").join("")??"";if(!output)throw new Error("Qwen returned an empty response.");return Response.json({output});}catch(error){return Response.json({error:error instanceof Error?error.message:"Qwen request failed."},{status:502})}}
