import { NextRequest } from "next/server";
export async function POST(request:NextRequest){const key=process.env.BITGET_QWEN_API_KEY;if(!key)return Response.json({error:"Qwen is not configured in this demo yet."},{status:503});const body=await request.json();const prompt=`You are Veyra, a market-structure research assistant. Explain these deterministic rToken execution metrics in no more than 120 words. Distinguish observed data from assumptions. Do not predict prices, invent facts, or tell the user to place an order. End with one piece of evidence that would change the conclusion.\n\n${JSON.stringify(body)}`;try{const response=await fetch("https://hackathon.bitgetops.com/v1/responses",{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${key}`},body:JSON.stringify({model:"qwen3.8-max",input:prompt})});const json=await response.json();if(!response.ok)throw new Error(json.error?.message||"Qwen request failed.");const output=json.output_text??json.output?.flatMap((item:{content?:Array<{text?:string}>})=>item.content??[]).map((item:{text?:string})=>item.text??"").join("")??"";if(!output)throw new Error("Qwen returned an empty response.");return Response.json({output});}catch(error){return Response.json({error:error instanceof Error?error.message:"Qwen request failed."},{status:502})}}
